# Prototype 26 — Follower Boundary-Demand Regulation Lifecycle

## Status

v4.7.53 non-canonical TEST BUILD over exact owner-declared canonical v4.7.49.

## Purpose

Test one implementation hypothesis exposed by the v4.7.52 failure: once follower Regulation is admitted to preserve leader-first / follower-second access to field-bounded boundary Action Space, the protection purpose must persist until positive inverse relationship evidence retires it. The numeric cap may change while the purpose remains live.

This prototype does **not** test the Provisional Demand Seed. It still requires an uncontaminated session-local native boundary demonstration before follower-maturation Regulation can admit. Bootstrapping is deliberately deferred so the lifecycle result is not confounded.

## Hypothesis

For an adjacent line-astern leader/follower progression:

1. positive demonstrated boundary demand plus settled/Productive follower geometry may admit bounded Regulation;
2. Regulation making the instantaneous geometry supportable must produce `REGULATION_MAINTAIN`, not purpose release;
3. loss of the original comparable straight-line representation while the leader reveals native continuation is uncertainty, not release authority;
4. the cap may be re-derived upward or downward while the same purpose persists;
5. release requires positive evidence from the same relationship that admitted protection: the continuations are positively no longer aligned, trailing order has ended, lateral demand is positively decoupled, or the Job Episode/context changes;
6. `turn=true`, an elapsed `t`, current positive reserve, and absence of current overlap evidence have no retirement authority.

The 0.99 heading-dot and three-coherent-sample gate remain temporary **admission-only** test mechanics. Reaction margin remains unresolved.

## Control boundary

The only active authority is the existing bounded P22 drive-speed ceiling. GIANTS retains route, steering, acceleration permission and forward/reverse choice. General production Control remains disabled.

## Player-facing evidence

While the purpose is active, a neutral HUD line reports:

`<Follower> — pacing for <Leader> boundary clearance • cap <n> km/h`

The HUD explains intervention; it supplies no Decision or release authority. Forensic geometry remains in the log.

## Live protocol

1. Verify `OuttaMyWay 4.7.53 • TEST BUILD` in the HUD.
2. Obtain the same clean uncontaminated native boundary-demand demonstration used by the preceding probes.
3. Repeat the adjacent Condor-leader / Patriot-follower progression.
4. Capture video and game log.
5. Inspect `FOLLOWER-COMPRESSION`, `REGULATION_APPLY`, `REGULATION_MAINTAIN`, `REGULATION_RELEASE`, `REGULATION_REFUSED`, and `DEMONSTRATION_REJECTED`.

## Pass evidence

A useful positive result is not merely “no collision”. Evidence should show one protection purpose admitted, maintained while Regulation itself makes current geometry supportable, retained through non-comparable leader transition evidence, adjusted without apply/release oscillation, and retired only on positive relationship/context evidence. The visible follower progression should remain as unobtrusive as the required ordering permits.

## Failure value

Deadlock with a continuously maintained purpose/cap would finally test the coarse geometry/reaction-margin hypothesis rather than repeating v4.7.52's lifecycle defect. Any premature release is a lifecycle failure and does not disprove the geometric demand model.
