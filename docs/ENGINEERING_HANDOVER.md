# Engineering Handover

> **Canonical baseline:** v4.7.15  
> **Candidate:** v4.7.17 Configuration-Filtered Plan-View Representation Shadow  
> **Control authority:** disabled

## Closed gates

Canonical v4.7.14 closed Field World identity for merged, split and contiguous fixtures. Canonical v4.7.15 isolated TS015 Encounter non-admission to absent scalar physical representation.

The v4.7.16 diagnostic candidate then proved Job Episode-scoped assembly and geometry caching, current component transformation and positive shadow conflict recognition. It was not declared canonical. Live review found that all cached collision-like primitives were inserted into every profile, allowing inactive alternative Condor shop geometry to inflate the tested 36 m assembly to approximately 54 m.

## v4.7.17 correction

`AssemblyRepresentationCache` now distinguishes:

```text
complete Job Episode geometry inventory
        ↓
material configuration profile
        ↓
currently participating primitives
        ↓
positioned plan-view footprint
```

`getIsCompoundChild` supplies principal runtime participation evidence. The complete Condor donor inventory classifies the 13 source-known 36 m/permanent identities separately from 16 inactive alternatives. A matching purchased configuration can support a donor fallback only if the runtime query is unavailable. Generic candidates receive no positive footprint authority unless runtime participation is confirmed.

Expensive local geometry calls remain one-time per Job Episode. Runtime activity checks occur only when a new profile is first encountered. Profile reuse transforms cached participating primitives through current poses.

## Live objective

Run the established TS015 setup and establish:

1. Condor selector evidence reports the purchased folding configuration;
2. complete Condor inventory is retained while inactive alternatives are excluded;
3. the deployed Condor span is broadly consistent with 36 m, not 54 m;
4. Patriot participation is independently runtime-confirmed;
5. geometry API measurement counts remain stable after initial discovery;
6. profile cache reuse remains stable;
7. shadow evidence remains isolated from Encounter admission and Control.

Do not promote the footprint path into live interaction evidence until this corrected gate passes.
