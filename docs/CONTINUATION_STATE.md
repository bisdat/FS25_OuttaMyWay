# Continuation State

Continuation State is the compact, replace-in-place description of the project's
present engineering boundary. Git history, pull requests, Issues and the
Engineering Journal preserve chronology.

## Repository authority

- Accepted Repository State: `main` after PR #125 merge,
  `308e27b465a887a065c944d5937f66aae01a1420`.
- Canonical authority remains **v0.3.0.0**.
- Accepted executable identity is
  **`0.3.0.51 TEST — DONOR TOPOLOGY RETIREMENT`**.
- Protected post-merge Offline Validation run #310 passed on the exact PR #125
  merge commit.
- Issue #112 is closed completed. The historical completed-worker donor topology
  and `POST_JOB_ACTUATION` contract are retired; current production has one
  provenance-neutral Causal Obstruction / Obstruction Relocation path.

## Current workstream — Issue #87

`scripts/config.lua` remains a **Mixed Runtime Constants Surface**. Issue #87 owns
its decomposition by actual responsibility and the eventual clear
Player / Developer-Debug / Internal presentation boundary.

The first ownership decision is now resolved:

> **Core Resolution Capability != Optional Configuration**

> **Master Enablement Is Sufficient Consent for Core Obstruction Relocation**

`AUTOMATIC_TERMINAL_EGRESS` is obsolete development scaffolding rather than a
player Configuration concept. Current Obstruction Relocation already has the
independent boundaries that determine legitimate action:

- fresh positive Causal Obstruction;
- `NON_ACTIVE_UNCLAIMED` blocker classification;
- current Player Claim veto;
- active GIANTS Job / source-AI precedence;
- current Physical Relocation Reference;
- Candidate / mandatory Constraint / Decision support;
- Bounded Authority and mechanical exclusivity;
- Control neutralisation and relinquishment; and
- fresh Situation Assessment after each bounded actuation.

Removing the historical consent gate must not weaken or duplicate any of those
boundaries.

The remaining legacy names
`TERMINAL_INTERIOR_SETTLEMENT_MAX_DISTANCE_M` and
`TERMINAL_EGRESS_MOVE_TIMEOUT_MS` still have live but distinct responsibilities:
bounded relocation distance policy and Control watchdog respectively. Their
ownership/name reconciliation remains later #87 work and is not part of the first
tranche.

## Next bounded increment

Implement **`0.3.0.52 TEST — OBSTRUCTION RELOCATION CORE CAPABILITY`**:

1. remove `AUTOMATIC_TERMINAL_EGRESS` from `scripts/config.lua`;
2. remove the two Candidate admission gates and their development-consent
   evidence annotation;
3. make Configuration architecture explicit that Obstruction Relocation is a
   core capability rather than a separate player option;
4. add focused behavioural/structural protection that relocation support no
   longer depends on a configuration gate;
5. preserve all `.51` obstruction classification, Player Claim, new-Job,
   bounded-inward recurrence, centroid exhaustion, serialization, authority and
   Control semantics.

GitHub Actions remains the independent offline validation authority. In-game
Reality validation is separate and should be requested only if review/CI exposes
a runtime-dependent uncertainty or the owner chooses an additional smoke
challenge.

## Separate open work

- **#123** — deferred GIANTS Reality validation: new Job and Player Claim during
  Obstruction Relocation.
- **#116** — Cooperative Passage crossing-window jam investigation.
- **#45** — Bubble Bullet Time accepted architecture, not implemented.
- **#89** — deferred HUD / player communication work.

Do not fold those workstreams into #87 for convenience.
